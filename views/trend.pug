extends layout.pug

block content
    - var Trend = trend
    .container
        .row.text-center.mb-5
            h1 #{trend.name}
        .row
            .col
                .row.mb-5 
                    .card.border-3
                        .card-body
                            h5.card-title Most Positive Tweet
                            p.card-text Louder than bombs💜💜💜💜#MyBTSTracks
                .row
                    .card.border-3
                        .card-body
                            h5.card-title Most Negative Tweet
                            p.card-text Am I Wrong #MyBTSTracks
            .col
                .row.text-center
                    h5 Tweet Count: #{trend.tweet_volume}
                    - var country = trend.lang === "tr" ? "Turkey" : "America"
                    h5 Country: #{country}
                .row.mb-5.text-center
                    canvas#myChart(style='width:100%; max-width:600px')

                script.
                    const trend = !{JSON.stringify(trend)};
                    var xValues = ["Negative", "Neutral", "Positive"];
                    var yValues = [((trend.neg_result * trend.tweet_volume) / 100).toFixed(0), ((trend.neu_result * trend.tweet_volume) / 100).toFixed(0),((trend.pos_result * trend.tweet_volume) / 100).toFixed(0)];
                    var barColors = [
                        "#F93535",
                        "#C0C0C0",
                        "#3DD6F5"
                    ];
                    new Chart("myChart", {
                        type: "pie",
                        data: {
                            labels: xValues,
                            datasets: [{
                                backgroundColor: barColors,
                                data: yValues
                            }]
                        },
                        options: {
                            title: {
                                display: true,
                                text: `${trend.name} Analysis`
                            }
                        }
                    });
